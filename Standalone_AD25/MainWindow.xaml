<Window x:Class="Standalone_AD25.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:h="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
        xmlns:local="clr-namespace:Standalone_AD25"
        mc:Ignorable="d"
        Title="Standalone AD25 - Fusion" Height="850" Width="1400" Background="#FF313131">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="BtnBack" Content="◀" Width="30" Click="BtnBack_Click" Margin="0,0,5,0"/>
                <TextBox x:Name="SearchBox" Width="300" Height="26" VerticalContentAlignment="Center" FontSize="14" KeyDown="SearchBox_KeyDown" Margin="0,0,10,0"/>
                <Button x:Name="BtnSearch" Content="Search HCSC" Width="100" Click="BtnSearch_Click"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="BtnExtractPdf" Content="📄 GET DATASHEET (PDF)" 
            FontWeight="Bold" Width="180" Height="30" Margin="0,0,10,0" 
            Click="BtnExtractPdf_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#FF007ACC"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#FF2D2D30"/>
                                    <Setter Property="Foreground" Value="#FF666666"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="BtnImportLib" Content="⬇ IMPORT LIB (CAD)" 
            FontWeight="Bold" Width="180" Height="30" 
            Click="BtnImportLib_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#FF4CAF50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#FF2D2D30"/>
                                    <Setter Property="Foreground" Value="#FF666666"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1">
                <ContentControl x:Name="ContentHost" />
            </Border>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#FF444444"/>

        <Grid Grid.Column="2" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="PartId" Text="No Component Loaded" IsReadOnly="True" Grid.Row="0" Margin="0,0,0,5" 
                     Background="#FF444444" Foreground="White" TextAlignment="Center" FontWeight="Bold"/>

            <Viewbox Grid.Row="1" Height="60" Margin="0,0,0,5">
                <Image x:Name="Thumbnail"/>
            </Viewbox>

            <GroupBox Header="Symbol" Grid.Row="2" Foreground="White" Margin="0,0,0,5">
                <ScrollViewer x:Name="SymbolCanvasView" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Background="Black">
                    <Canvas x:Name="SymbolCanvas" Width="500" Height="500"/>
                </ScrollViewer>
            </GroupBox>

            <GroupBox Header="Footprint" Grid.Row="3" Foreground="White" Margin="0,0,0,5">
                <ScrollViewer x:Name="FootprintCanvasView" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Background="Black">
                    <Canvas x:Name="FootprintCanvas" Width="500" Height="500"/>
                </ScrollViewer>
            </GroupBox>

            <GroupBox Header="3D Model" Grid.Row="4" Foreground="White">
                <h:HelixViewport3D x:Name="ModelView" 
                       ZoomExtentsWhenLoaded="True" 
                       ShowViewCube="False" 
                       ShowCoordinateSystem="True"
                       Background="Black">
                    <h:DefaultLights />
                </h:HelixViewport3D>
            </GroupBox>
        </Grid>
    </Grid>
</Window>